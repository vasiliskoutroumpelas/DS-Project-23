Run *GNU Make* on the terminal in the project directory to build the project. The executable will be located to the *bin* directory.
If not on Linux, you might need to create the directories *bin/* and *obj/*.
